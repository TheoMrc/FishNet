# Usage: tox -e format
[tox]
envlist = format

[testenv:format]
description = Format and type-check with Ruff and Ty
skip_install = true
deps =
    ruff>=0.11
    ty>=0.0.1
commands =
    ruff format .
    ruff check . --fix
    ty check
