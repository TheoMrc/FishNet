const button=document.getElementById('save-rolling');button?.addEventListener('click',async()=>{const updates=[...document.querySelectorAll('#rolling-table tbody tr[data-frame]')].map(row=>({experiment:row.dataset.experiment,video:row.dataset.video,frame:row.dataset.frame,fish_index:Number(row.dataset.fishIndex),rolling:row.querySelector('input').checked}));const response=await fetch('/rolling/save',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(updates)});const data=await response.json();if(!response.ok)alert(data.message||'Save failed');else alert('Rolling labels saved')});
